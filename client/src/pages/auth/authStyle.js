import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
      primary: {
        light: '#B4B198',
        main: '#455442',
      },
      secondary: {
        light: 'rgb(180, 184, 193)',
        main: '#fff' 
      }
    }
});